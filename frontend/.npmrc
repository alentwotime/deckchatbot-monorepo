# Force official public npm registry (fixes network issues in Codex/Docker)
registry=https://registry.npmjs.org/

# Prevent unnecessary auth prompts or 401 errors
always-auth=false

# Speeds up installs by reducing progress logs (optional)
progress=false

# Use strict SSL (good for Render/Codex)
strict-ssl=true

# Optional: use global cache to avoid redundant downloads
cache=${HOME}/.npm-cache
